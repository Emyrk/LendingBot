# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
GET     /sand                                   App.Sandbox
POST	/login									App.Login
GET		/logout									AppAuthRequired.Logout
POST	/register								App.Register
GET		/dashboard								AppAuthRequired.Dashboard
GET		/dashboard/info							AppAuthRequired.InfoDashboard
GET		/dashboard/info/:id						AppAuthRequired.InfoAdvancedDashboard
GET		/dashboard/settings						AppAuthRequired.SettingsDashboard
GET		/dashboard/sysadmin						AppAuthRequired.SysAdminDashboard
GET		/dashboard/admin						AppAuthRequired.AdminDashboard

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
